# Final visual gate — rejected round 1

Three independent reviewers passed Azure Navigator and Heartline Noir, but all three rejected Lemon Cadet before installation.

The Lemon `running-right` row contained detached anatomy or footwear fragments in native-size cells `r1c1`, `r1c2`, and `r1c6`. Deterministic atlas, chroma, transparency, and direction validators had passed, which demonstrates that structural validation alone does not cover connected-component semantics.

Required repair: regenerate the complete grounded `running-right` row from the identity/canonical references, never patch individual cells, rebuild the pre-despill atlas, and rerun final validation plus independent visual QA. The rejected atlas is retained here for traceability and was never installed.
